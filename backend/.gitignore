node_modules
# Keep environment variables out of version control
.env

/../generated/prisma

# SQLite is no longer used (Postgres/Supabase via DATABASE_URL); ignore any local leftovers
*.db
*.db-journal
